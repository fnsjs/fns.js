// @flow
// This file is generated automatically by `scripts/build/typings.js`. Please, don't change it.

type CurriedFn1<A, R> = <A>(a: A) => R

type CurriedFn2<A, B, R> = <A>(
  a: A
) => CurriedFn1<B, R> | (<A, B>(a: A, b: B) => R)

type CurriedFn3<A, B, C, R> = <A>(
  a: A
) =>
  | CurriedFn2<B, C, R>
  | (<A, B>(
      a: A,
      b: B
    ) => CurriedFn1<C, R> | (<A, B, C>(a: A, b: B, c: C) => R))

type CurriedFn4<A, B, C, D, R> = <A>(
  a: A
) =>
  | CurriedFn3<B, C, D, R>
  | (<A, B>(
      a: A,
      b: B
    ) =>
      | CurriedFn2<C, D, R>
      | (<A, B, C>(
          a: A,
          b: B,
          c: C
        ) => CurriedFn1<D, R> | (<A, B, C, D>(a: A, b: B, c: C, d: D) => R)))

declare module.exports: {
  castArray: CurriedFn1<any, Array<any>>,
  fromArray: CurriedFn2<any, any, Array<any>>,
  isArray: CurriedFn1<any, boolean>,
  isBoolean: CurriedFn1<any, boolean>,
  isDate: CurriedFn1<any, boolean>,
  isError: CurriedFn1<any, boolean>,
  isFunction: CurriedFn1<any, boolean>,
  isLength: CurriedFn1<any, boolean>,
  isNull: CurriedFn1<any, boolean>,
  isNumber: CurriedFn1<any, boolean>,
  isObject: CurriedFn1<any, boolean>,
  isObjectLike: CurriedFn1<any, boolean>,
  isPlainObject: CurriedFn1<any, boolean>,
  isRegExp: CurriedFn1<any, boolean>,
  isString: CurriedFn1<any, boolean>,
  filter: CurriedFn2<(...args: Array<any>) => any, Array<any>, Array<any>>,
  last: CurriedFn1<any, boolean>,
  map: CurriedFn2<(...args: Array<any>) => any, Array<any>, Array<any>>,
  max: CurriedFn1<Array<any>, any>,
  min: CurriedFn1<Array<any>, any>,
  pull: CurriedFn1<any, boolean>,
  reduce: CurriedFn3<any, (...args: Array<any>) => any, Array<any>, any>,
  reverse: CurriedFn1<any, boolean>,
  takeRight: CurriedFn2<number, Array<any>, Array<any>>,
  isArrayLike: CurriedFn1<any, boolean>,
  MAX_SAFE_INTEGER: number,
}
